import { getTranslations } from 'next-intl/server';
// import type { Metadata } from 'next'
import Posted from './posted';

export const metadata = async () => {
  const langTrans = await getTranslations('lang');
  return {
    title: `${langTrans('posted/title')} | Next Test App`,
    description: `${langTrans('posted/title')} | Next app, generated by create next app`,
  }
}

export default async function Home() {
  const langTrans = await getTranslations('lang');
  return (
    <>
      <h1 className="title">{langTrans('posted/title')}</h1>
      <Posted/>
    </>
  )
}
